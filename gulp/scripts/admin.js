$(function() {
    /*$('.search').on('keyup', '.mobile', function() {
        // console.log($(this).val())
        // $('.search a').attr('href', "?player=" + $(this).val());
        $('form').attr('action', "?user=" + $(this).val());
    })*/
})
